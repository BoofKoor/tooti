/**
 * Auth.js (NextAuth) configuration — wired in Phase 3 (Data & auth).
 *
 * Intentional placeholder: it reserves the file from handoff §3 without pulling
 * auth dependencies into the Phase 0 foundation. The provider choice
 * (email magic-link / Google / both) is a Phase 3 decision per the roadmap.
 */
export {};
